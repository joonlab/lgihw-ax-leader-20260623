/* ========================================
   PANEL SYSTEM (no-op stubs for static build)
   ======================================== */
export function openPanel() { /* no-op: panels removed in static build */ }
export function closePanel() { /* no-op */ }
window.openPanel = openPanel;
window.closePanel = closePanel;
